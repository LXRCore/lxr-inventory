# Item images

Drop PNG files named after the item (bread.png, weapon_revolver_cattleman.png) into this folder.
The UI falls back to a lettered tile when an image is missing. Keep files small (under 30 KB) — CEF memory is limited.
